database name: database123
"database tables:"
user_db = user_id, fname(30), username(15), email(30), password(55)  // user_id Int autoIncre.  varchar all


"user account"
user: admin
: localhost
password: admin


"how to connect to use db"
//connect your php to db
include 'connect_db.php'; //include this file

//create variable and add sql command
$sql = "INSERT INTO users_db(user_id, fname, username, email, password)
VALUES ('', '$fname', '$username', '$email', SHA1('$password'))";

//run the command
$conn->exec($sql);

//close the connection
$conn = null;
